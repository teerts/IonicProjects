import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic-project.recipelist-project',
  appName: 'ionic-test-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
